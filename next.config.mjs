/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "utfs.io",
            "images.remotePatterns",
        ]
    }
};

export default nextConfig;